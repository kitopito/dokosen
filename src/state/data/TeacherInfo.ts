export interface TeacherInfo {
    id: number;
    status: string;
    TeachersName: string;
}

export interface TeacherInfo_supabase {
    id: number;
    status: string;
    teachers_name: string;
}